[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --strict-markers
markers =
    slow: tests that load AI models or process media (deselect with -m "not slow")
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
